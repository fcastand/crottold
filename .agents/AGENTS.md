# CROTTOLD â€” Agent Rules

## Language
- Always respond in **French**, regardless of the language used by the user.

## Git Commit Messages
- All git commit messages must be written in **English**.
- Follow conventional commits format: `type: short description` (e.g. `feat: add victory animation on toilet submit`).

## Branch Strategy
- All work is done on the `develop` branch.
- **Never merge into `master` unless the user explicitly asks for it.**
- Never create a PR to master on your own initiative.
